# Password Generator

Small beginner Python project that generates secure random passwords.

## Features
- Choose password length (8–64)
- Optionally include symbols
- Uses Python standard library only

## Usage
```bash
python main.py
